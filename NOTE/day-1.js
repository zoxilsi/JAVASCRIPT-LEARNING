variable and constant
  <> A "data container" / "data storage"
  <> Where the value can change!
      let
      var
      const
  <> Use constant as often as possible 
  <> Whenever you actually got data that never changes
  <> To be clear about your intentions (in your code).
